<!DOCTYPE html>
<html lang="en">
<head>
    <title>{% block title %} {% endblock %} -MySite</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

</head>
<body class="d-flex flex-column min-vh-100">
<nav class="navbar navbar-expand-sm bg-dark navbar-dark sticky-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">MySite</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <div class="d-flex justify-content-between w-100">
                <ul class="navbar-nav">
                    {% if route_exists('admin') %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Admin</a>
                    </li>
                    {% endif %}
                    {% if route_exists('posts') %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Posts</a>
                    </li>
                    {% endif %}

                </ul>
                <ul class="navbar-nav">
                    {% if route_exists('inscription') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('inscription') }}">Inscription</a>
                    </li>
                    {% endif %}
                    {% if route_exists('connexion') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('connexion') }}">Login</a>
                    </li>
                    {% endif %}
                    {% if route_exists('logout') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('logout') }}">Logout</a>
                    </li>
                    {% endif %}
                </ul>
            </div>

        </div>
    </div>
</nav>


<main class="container-fluid px-5 flex-grow-1 mt-5 ">
 <div class="row">
     <div class="col-md-9" style="margin: auto;">
         {% set types=['success','error','info','warning'] %}
         {% for type in types%}
             {% set message= flash(type) %}
             {% if message %}
                 <div class="alert alert-{{ type}} alert-dismissible">
                     <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                     {{ message }}
                 </div>
             {% endif %}
         {% endfor %}
     </div>
 </div>
 {% block body %}{% endblock %}
</main>
<footer>
    <p class=" text-center lead">&copy;{{ 'now'|date('Y')}} MySite</p>
</footer>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
